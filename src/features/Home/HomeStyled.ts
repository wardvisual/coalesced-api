import { styled } from '@/shared/theme';

const HomeWrapper = styled.div`
  display: grid;
  place-items: center;
  gap: 2em;

  > div {
    display: flex;
    gap: 1em;
  }
`;

export default HomeWrapper;
